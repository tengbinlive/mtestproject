<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/img"
        android:layout_width="@dimen/sticker_image_size"
        android:layout_height="@dimen/sticker_image_size"
        android:layout_below="@+id/delete"
        android:layout_toRightOf="@+id/delete"
        android:scaleType="centerInside"
        android:src="@mipmap/ic_launcher" />

    <ImageView
        android:id="@+id/delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/sticker_delete" />

    <ImageView
        android:id="@+id/flip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/img"
        android:src="@mipmap/sticker_flip" />

    <ImageView
        android:id="@+id/scale_rotation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/img"
        android:layout_toRightOf="@+id/img"
        android:src="@mipmap/sticker_scale" />


</RelativeLayout>
