<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.binteng">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.AnimationLaunchActivity" />

        <activity android:name=".activity.AnimationSeachActivity" />

        <activity android:name=".activity.AudioFrequencyActivity" />

        <activity android:name=".activity.StickerActivity" />

        <activity android:name=".activity.AnimationTabActivity" />

        <activity android:name=".activity.TabBarActivity" />
    </application>

</manifest>